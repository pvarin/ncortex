''' A module containing useful utilities that are likely to be reused.
'''

from .distance_metrics import (angle_distance, angle_diff,
                               squared_angle_distance)

from .matrix import (is_pos_def)
